node_modules
.next
.vercel
.env
.env.local
.env.*.local
!.env.example
coverage
dist
.venv
__pycache__
.pytest_cache
.mypy_cache
.ruff_cache
*.log
*.tsbuildinfo
.DS_Store
/out
# Convex snapshots: full copies of production personal data.
backups

# Personal Claude Code overrides. The shared hook lives in .claude/settings.json.
.claude/settings.local.json

# Convex agent skills are vendored by `bunx convex ai-files install` and pinned
# by skills-lock.json, so they are regenerated rather than committed — the same
# reasoning as node_modules. The project's own skill (.claude/skills/firstcontact)
# is authored here and IS committed.
.claude/skills/convex*/
.agents/skills/convex*/
